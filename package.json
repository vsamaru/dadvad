{
    "name": "myapp",
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "src/index.ts",
    "scripts": {
        "g": "git add . && git commit -m 'Initial commit' && git branch -M main && git push -f",
        "build": "wrangler build",
        "format": "npm run prettier:base -- --write '*.ts'"
    },
    "author": "vsamaru <vsamaru@gmail.com>",
    "license": "ISC",
    "devDependencies": {
        "@cloudflare/workers-types": "^2.2.1",
        "@types/node": "^12.0.8",
        "prettier": "^1.17.0",
        "ts-loader": "^8.0.18",
        "types-cloudflare-worker": "^1.0.0",
        "typescript": "^3.5.1"
    },
    "dependencies": {
        "@cloudflare/wrangler": "^1.15.0"
    }
}
